apiVersion: v1
kind: Service
metadata:
  name: {{ include "ct-lucas-olopes.fullname" . }}
  labels:
    {{- include "ct-lucas-olopes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ct-lucas-olopes.selectorLabels" . | nindent 4 }}
