apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ct-lucas-olopes.fullname" . }}-test-connection"
  labels:
    {{- include "ct-lucas-olopes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ct-lucas-olopes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
